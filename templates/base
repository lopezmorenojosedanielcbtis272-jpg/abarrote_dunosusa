<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Abarrote Duñosusa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .producto-card {
            transition: transform 0.2s;
        }
        .producto-card:hover {
            transform: translateY(-5px);
        }
        .carrito-badge {
            position: absolute;
            top: -5px;
            right: -5px;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="{{ url_for('index') }}">🏪 Abarrote Duñosusa</a>
            
            <div class="navbar-nav ms-auto">
                {% if usuario %}
                    <span class="navbar-text me-3">Bienvenido, {{ usuario }}</span>
                    <a class="nav-link position-relative" href="{{ url_for('ver_carrito') }}">
                        🛒 Carrito
                        {% if session.get('carrito') %}
                            <span class="badge bg-danger carrito-badge">{{ session.carrito|length }}</span>
                        {% endif %}
                    </a>
                    <a class="nav-link" href="{{ url_for('logout') }}">Cerrar Sesión</a>
                {% else %}
                    <a class="nav-link" href="{{ url_for('registro') }}">Registrarse</a>
                {% endif %}
            </div>
        </div>
    </nav>

    <!-- Contenido principal -->
    <div class="container mt-4">
        {% block content %}{% endblock %}
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-white mt-5 py-4">
        <div class="container text-center">
            <p>&copy; 2024 Abarrote Duñosusa. Todos los derechos reservados.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
